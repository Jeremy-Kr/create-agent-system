name: full-team
description: "For large projects. All agents, full review cycle, strict Visual QA."
scale: large

agents:
  po-pm: { enabled: true, model: opus }
  architect: { enabled: true, model: opus }
  cto: { enabled: true, model: opus }
  designer: { enabled: true, model: opus }
  test-writer: { enabled: true, model: opus }
  frontend-dev: { enabled: true, model: opus }
  backend-dev: { enabled: true, model: opus }
  qa-reviewer: { enabled: true, model: opus }

workflow:
  review_max_rounds: 5
  qa_mode: standard
  visual_qa_level: 3
  epic_based: true

skills:
  - scoring
  - visual-qa
  - tdd-workflow
  - adr-writing
  - ticket-writing
  - design-system
  - cr-process
  - sync-spec
